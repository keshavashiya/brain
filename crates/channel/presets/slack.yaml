id: slack
kind: webhook_outbound
label: Slack

# Slack incoming webhooks carry the secret in the URL path. The full
# webhook URL is provided via `credential`.
send:
  url_template: "{credential}"
  method: POST
  content_type: "application/json"
  body_template: '{"text":"{content}"}'
