# opencode. `opencode run -` reads the prompt from stdin.
id: opencode
binary_names: [opencode]
version_args: ["--version"]
args: ["run", "-"]
prompt_via_stdin: true
capabilities:
  tags: [code-edit]
  max_concurrency: 1
  needs_network: true
