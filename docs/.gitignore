# Rendered book (already ignored at repo root, repeated here for clarity)
book/

# Docs-assistant build artifacts — regenerated by CI / the local build steps in
# README.md. Not committed: they are derived from src/**.md + the WASM crate.
tools/node_modules/
tools/.cache/
src/assistant/pkg/
src/assistant/brain-index.json
